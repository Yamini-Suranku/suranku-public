{
  "categories": [
    { "id": "source", "label": "Source & contracts", "heading": "Where data comes from", "blurb": "How producing systems agree on the shape and meaning of the data they publish." },
    { "id": "ingestion", "label": "Ingestion & process", "heading": "How data is brought in", "blurb": "The steps that turn raw published events into trustworthy, query-ready data." },
    { "id": "storage", "label": "Storage & traceability", "heading": "Where data lands and how it's traced", "blurb": "The historical layers data is written to, and the trails that explain it." },
    { "id": "operations", "label": "Operations & AI", "heading": "Running and assisting the platform", "blurb": "How the apps stay healthy, and how AI answers are produced responsibly." }
  ],
  "apps": [
    { "id": "core", "label": "Core concepts" },
    { "id": "data-intelligence-portal", "label": "Data Intelligence Portal" }
  ],
  "terms": [
    { "term": "Domain", "category": "source", "app": "core",
      "definition": "A source-system area of ownership (e.g. <em>Retail Commerce</em>) that publishes an agreed set of events. Domains keep ownership and responsibility clear as the platform grows." },
    { "term": "Data contract", "category": "source", "app": "core",
      "definition": "A formal agreement from a producer describing an event: its topic, name, version, primary keys, and schema. Contracts let consumers rely on data without surprises when producers change." },
    { "term": "Kafka topic", "category": "source", "app": "core",
      "definition": "A named stream that events are published to (e.g. <code>commerce.orders.created</code>). Apache Kafka is a common backbone for moving event data between systems in near real time." },
    { "term": "Event", "category": "source", "app": "core",
      "definition": "A single record describing something that happened — an order created, a payment captured. Platforms are built by collecting and processing streams of events over time." },
    { "term": "Protobuf (Protocol Buffers)", "category": "source", "app": "core",
      "definition": "A compact, strongly-typed format for defining the structure of an event. A <code>.proto</code> schema acts as the contract's blueprint so producers and consumers agree on every field." },
    { "term": "Schema", "category": "source", "app": "core",
      "definition": "The defined structure of a record — its fields and their types. Schemas make data predictable and let tools validate it." },
    { "term": "Primary key", "category": "source", "app": "core",
      "definition": "The field (or fields) that uniquely identify a record, such as <code>order_id</code>. Primary keys are how the platform recognises duplicates and links related data." },

    { "term": "Marker", "category": "ingestion", "app": "data-intelligence-portal",
      "definition": "A small signal file that says \"a batch of data is ready to process.\" Markers prevent ingestion from starting on half-published data." },
    { "term": "Marker-driven ingestion", "category": "ingestion", "app": "data-intelligence-portal",
      "definition": "Ingestion that begins only after a marker appears, rather than on a blind timer. It ensures a complete, consistent batch is processed." },
    { "term": "Ingestion run", "category": "ingestion", "app": "data-intelligence-portal",
      "definition": "One execution of the ingestion process for a contract: it reads events, removes duplicates, writes catalog tables, and records what happened as evidence." },
    { "term": "Deduplication (dedup)", "category": "ingestion", "app": "core",
      "definition": "Removing repeated records that share the same primary key, so each real-world thing is counted once. Streams often deliver the same event more than once, so dedup is essential for correct numbers." },
    { "term": "Idempotency", "category": "ingestion", "app": "core",
      "definition": "The property that running the same operation again produces the same result — re-running an ingestion doesn't create duplicate output. It makes pipelines safe to retry." },

    { "term": "Catalog table", "category": "storage", "app": "data-intelligence-portal",
      "definition": "A processed, query-ready table named <code>&lt;layer&gt;.&lt;domain&gt;.&lt;event&gt;</code>. The catalog is where downstream analysts and apps read data." },
    { "term": "Catalog layers", "category": "storage", "app": "data-intelligence-portal",
      "definition": "<strong>Intraday</strong> — fresh data as it arrives during the day. <strong>End-of-day</strong> — the closed, settled historical state for a day. <strong>Analytics</strong> — reporting-ready data shaped for dashboards and analysis." },
    { "term": "Object store", "category": "storage", "app": "data-intelligence-portal",
      "definition": "Scalable file storage (e.g. S3, MinIO) where the underlying data files live. The catalog points to data kept in the object store." },
    { "term": "Data lineage", "category": "storage", "app": "data-intelligence-portal",
      "definition": "The map of where data came from — tracing a catalog table back through its transformations to the original source topic. Answers \"where does this number come from?\"" },
    { "term": "Process lineage", "category": "storage", "app": "data-intelligence-portal",
      "definition": "The trail of <em>how</em> a batch was processed: marker discovered → records deduplicated → catalogs written. Answers \"what happened in this run, and why?\"" },

    { "term": "Liveness", "category": "operations", "app": "core",
      "definition": "A health check that asks \"is the service running at all?\" If it fails, the service is restarted." },
    { "term": "Readiness", "category": "operations", "app": "core",
      "definition": "A deeper check that asks \"is the service ready to do real work?\" — its database, storage, and dependencies are all available. A service can be live but not yet ready." },
    { "term": "Backend vs static mode", "category": "operations", "app": "data-intelligence-portal",
      "definition": "<strong>Backend mode</strong> persists data in a real API/database. <strong>Static mode</strong> runs the same UI entirely in your browser (using local storage) for the public demo — no server required." },
    { "term": "AI provider", "category": "operations", "app": "data-intelligence-portal",
      "definition": "The service that produces AI answers. The portal prefers native <strong>Claude/Anthropic</strong>, falls back to any <strong>OpenAI-compatible</strong> endpoint, and finally to a <strong>deterministic</strong> local responder — so it always works, with or without an API key." },
    { "term": "Deterministic answer", "category": "operations", "app": "data-intelligence-portal",
      "definition": "A built-in response generated from local metadata using fixed rules (no external AI). It's predictable and runs offline, which is why it's the always-available fallback." }
  ]
}
